extends ../layout

block contenido
    .container
        h1 Agregar nueva Especialidad

        //- Display success message if a new especialidad was added
        if success
            .alert.alert-success(role="alert") ¡Especialidad agregada exitosamente!

        form(action="/especialidades/store" method="POST")
            .mb-3
                label(for="nombre" class="form-label") Nombre:
                input(type="text" name="nombre" id="nombre" class="form-control" required)
            button(type="submit" class="btn btn-primary") Agregar Especialidad